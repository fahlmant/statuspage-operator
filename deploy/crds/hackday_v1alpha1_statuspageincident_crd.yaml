apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: statuspageincidents.hackday.openshift.io
spec:
  group: hackday.openshift.io
  names:
    kind: StatusPageIncident
    listKind: StatusPageIncidentList
    plural: statuspageincidents
    singular: statuspageincident
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            components:
              items:
                type: string
              type: array
            endTime:
              description: UTC
              type: object
            message:
              description: UTC
              type: string
            name:
              type: string
            startTime:
              type: object
            type:
              type: string
          required:
          - startTime
          - endTime
          - message
          - type
          - components
          - name
          type: object
        status:
          properties:
            scheduled:
              type: boolean
          required:
          - scheduled
          type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
